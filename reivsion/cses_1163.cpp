#include<bits/stdc++.h>
#define ll long long

using namespace std;

void solve(){
    int x,n;
    cin>>x>>n;
    int a[n];
    for (int  i = 0; i < n; i++)
    {
        cin>>a[i];
    }
    
}

int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    freopen("B008.inp","r",stdin);
    freopen("B008.out","w",stdout);
    
    return 0;
}